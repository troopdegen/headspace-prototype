{
  "name": "shape-server",
  "version": "0.1.0",
  "private": true,
  "description": "Persistence + k-anonymous community clustering for the shape prototype. Zero dependencies.",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js"
  },
  "engines": { "node": ">=18" }
}
